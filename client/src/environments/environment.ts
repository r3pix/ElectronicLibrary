// Production Api URL is not known yet (Api/SPA aren't deployed — see CLAUDE.md §9/§11); fill this in
// once the Api's Container Apps URL exists.
export const environment = {
  production: true,
  apiBaseUrl: ''
};
